<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
					xmlns:gantt="clr-namespace:Syncfusion.Windows.Controls.Gantt;assembly=Syncfusion.Gantt.Wpf"
					xmlns:local="clr-namespace:Clio.ProjectManagerDemo.WPF" 
					xmlns:vm="clr-namespace:Clio.ProjectManager.WPF.ViewModel" 
					>
	<DataTemplate DataType="{x:Type vm:TaskContent}">
		<Grid   HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Gainsboro" >
			<Border Margin="20" Background="White" CornerRadius="1" Padding="10" >
				<Border.Effect>
					<DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.4"/>
				</Border.Effect>
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="*"/>
					</Grid.RowDefinitions>
					<syncfusion:SfTextBoxExt Grid.Row="0"
								  HorizontalAlignment="Left" VerticalAlignment="Center" 
                                  HorizontalContentAlignment="Center"
                                  Height="40" Width="500" Margin="0,20"
								  Foreground="Maroon" FontWeight="DemiBold"
								  IsReadOnly="True"
                                  Text="Syncfusion Gantt control with simulated data added for demonstration purpose"/>
					<gantt:GanttControl x:Name="ganttTab" Grid.Row="1"
                                         HorizontalAlignment="Stretch"
                                         VerticalAlignment="Stretch"
									     StartTime="{Binding TimelineStart}"
                                         Width="Auto"										
										ItemsSource="{Binding TaskCollection}">
					</gantt:GanttControl>
				</Grid>
			</Border>
		</Grid>
	</DataTemplate>
</ResourceDictionary>